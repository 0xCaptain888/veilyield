# Copy to frontend/.env. These are read by Vite at build time (must start with VITE_).

# Optional dedicated Sepolia RPC for the relayer (more reliable than the public default).
VITE_SEPOLIA_RPC_URL="https://ethereum-sepolia-rpc.publicnode.com"
